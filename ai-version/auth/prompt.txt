Build authentication for a Python FastAPI task API using Supabase Auth. Add signup, login, logout, public info, protected profile, and protected dashboard routes. Return 201, 200, 204, 400, and 401 exactly as appropriate. Verify bearer access tokens with Supabase in one reusable dependency, reject malformed and tampered tokens, load the Supabase URL and anon key only from a git-ignored .env file, never log passwords or tokens, and add HTTPBearer so Swagger shows lock icons. Distinguish invalid credentials from a Supabase outage, returning 503 for the outage. Keep this generated comparison separate from the main implementation.
